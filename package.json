{
  "name": "donation-widget",
  "version": "1.0.0",
  "description": "a simple donation widget which does not connect to a backend system",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vinit Sood (Netlight)",
  "license": "ISC",
  "dependencies": {
    "connect": "^3.6.0",
    "serve-static": "^1.11.2"
  }
}
